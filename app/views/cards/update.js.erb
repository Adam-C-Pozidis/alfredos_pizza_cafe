// This partial is called uppon when we are updating a card and a shopping cart.

$('#cart').html("<%= j (render partial: 'shopping_carts/cart') %>")
$('#cart-item').html("<%= j (render partial: 'shopping_carts/cart_items') %>")
